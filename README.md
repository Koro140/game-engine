# Game Engine in Modern C++

It uses a Core/App architecture.  
Check the `App` folder for the example application.

## Build Instructions

The project uses CMake to build the project:

```bash
cmake -S . -B build
cd build
cmake --build build
```